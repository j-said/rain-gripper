in mqtt-connect
changed name: mqtt-connect -> mqtt_listener
changed: client -> user 
mqtt topic refactored: clients/client_id/sub_device/data_type (old) -> user_id/device_group/data_type
now listens all topics: clients/# (old) -> #
made new logic to deserialize JSON payload into devices

in database:
new DB tables: users, devise-groups, devises, sensors-data

in schemas:
changed: customer -> user 
updated in sync with DB

new directory testing
new module seed-db:
usage to fill db for tests


to send data with mqtt from shit-stick:
user/u-gruop/logs/{"devid1":{"humidity": float, "water_level": float}, "devid2":{}, "devidN":{}}